# 채용 면접 도우미 — 실행 방법

이 문서는 컴퓨터 프로그램을 처음 다루는 분도 그대로 따라 할 수 있도록 썼습니다.
차례대로 읽고 따라 하시면 됩니다.

---

## 1. 설치할 프로그램

**없습니다.**

윈도우 노트북에 이미 들어 있는 **크롬(Chrome)** 또는 **엣지(Edge)** 만 있으면 됩니다.
따로 내려받거나 설치할 것은 하나도 없습니다.

준비물은 두 가지뿐입니다.

| 준비물 | 설명 |
|---|---|
| `채용면접도우미.html` 파일 | 이 앱 전체가 들어 있는 파일 하나 |
| Google Gemini API 키 | AI에게 일을 시키기 위한 열쇠 (아래 2번에서 발급) |

> **파일이 두 개인 이유**
> `채용면접도우미.html` 과 `index.html` 은 **내용이 완전히 같은 파일**입니다.
> - 내 PC에서 두 번 클릭해 쓸 때 → `채용면접도우미.html`
> - 인터넷 주소로 올려서 쓸 때(넷리파이 등) → `index.html` 이 있어야 첫 화면이 열립니다.
>
> 둘 중 아무거나 하나만 쓰시면 되고, 팀장님께는 `채용면접도우미.html` 하나만 보내시면 됩니다.

---

## 2. Google Gemini API 키 발급 방법

API 키는 **"이 사람이 구글 AI를 써도 된다"는 것을 확인해 주는 긴 암호 문자열**입니다.
아래 순서대로 하시면 5분 안에 받을 수 있습니다.

1. 크롬이나 엣지를 켜고 주소창에 다음을 입력합니다.
   **https://aistudio.google.com/apikey**
2. 구글 계정으로 로그인합니다. (회사 계정 또는 개인 지메일 계정 모두 됩니다)
3. 처음 들어가면 이용 약관에 동의하라는 화면이 나올 수 있습니다. 내용을 확인하고 동의합니다.
4. 화면에 있는 **`Create API key`**(또는 `API 키 만들기`) 버튼을 누릅니다.
5. 프로젝트를 고르라는 창이 나오면, 이미 있는 것을 고르거나
   **`Create API key in new project`**(새 프로젝트에 만들기)를 누릅니다.
6. `AIza...` 로 시작하는 긴 글자가 나타납니다. 이것이 API 키입니다.
   옆의 **복사 아이콘(📋)** 을 눌러 복사합니다.
7. 이 키는 **비밀번호와 같습니다.** 다른 사람에게 알려 주지 마세요.
   메모장 등에 붙여 넣어 따로 보관해 두면 나중에 다시 쓸 때 편합니다.

### 비용 안내

- 개인 사용 수준에서는 **무료 사용량 안에서 쓸 수 있습니다.**
  (월 2~5회, 한 번에 30분 정도 쓰는 사용량이면 대부분 무료 범위입니다)
- 다만 무료 사용량과 요금 정책은 구글이 언제든 바꿀 수 있습니다.
  결제 수단을 등록한 계정에서 무료 한도를 넘겨 쓰면 요금이 나올 수 있으니,
  정확한 최신 내용은 발급 화면의 요금(Pricing) 안내를 한 번 확인해 주세요.

---

## 3. 앱 실행 방법

1. `채용면접도우미.html` 파일을 **바탕화면** 또는 **내 문서** 같이 찾기 쉬운 곳에 둡니다.
   (회사 공용 폴더나 USB에 두어도 됩니다)
2. 파일을 **마우스로 두 번 클릭**합니다.
3. 크롬이나 엣지가 열리면서 앱 화면이 나타납니다. 이것으로 실행 끝입니다.

> **다른 브라우저로 열렸다면**
> 파일 위에서 마우스 오른쪽 버튼 → `연결 프로그램` → `Chrome` 또는 `Edge` 를 고르세요.
> "항상 이 앱으로 열기"에 체크해 두면 다음부터는 두 번 클릭만으로 열립니다.

### 처음 한 번만 하는 설정

1. 왼쪽 메뉴에서 **⚙️ 설정** 을 누릅니다.
2. **Gemini API 키** 칸에 위에서 복사한 키를 붙여 넣습니다. (`Ctrl` + `V`)
3. **회사명** 칸에 회사 이름을 적습니다. (채용공고를 만들 때 쓰입니다. 비워도 됩니다)
4. **저장하기** 를 누릅니다.
5. **연결 테스트** 를 눌러 초록색으로 "연결에 성공했습니다"가 나오면 준비 완료입니다.

키는 이 브라우저 안에만 저장되므로, 다음에 열 때는 자동으로 채워져 있습니다.

### 사용 순서 (권장)

```
① 직무 정보 → 직무기술서 붙여넣고 저장
      (직무기술서가 없으면 "직무기술서가 없어요" 버튼으로 7개 질문에 답하기)
② 면접 질문 + 평가표 → "질문 만들기" → 워드로 내려받아 면접관에게 배포
③ 이력서 적합도 → 지원자별 이력서 분석 (선택)
④ 후보자 평가 → 면접 중 점수 입력 → 저장
⑤ 후보자 평가 → 비교표에서 총점순 확인
⑥ 채용공고 초안 → 공고 문구 만들기 (선택)
```

처음 써 보실 때는 왼쪽 아래 **🧪 샘플 데이터 불러오기** 를 눌러
가상의 직무와 후보자로 미리 연습해 보시길 권합니다.
연습이 끝나면 **🗑️ 전체 데이터 지우기** 로 정리하면 됩니다.

---

## 4. 팀장님들에게 나눠 주는 방법

**`채용면접도우미.html` 파일 하나만 전달하면 됩니다.**
메일 첨부, 사내 메신저, 공용 폴더, USB 어느 방법이든 괜찮습니다.

다만 아래 두 가지를 꼭 안내해 주세요.

### ① API 키는 각자 필요합니다

- 이 앱은 **각자의 PC 브라우저에 각자의 키를 저장**합니다.
- 담당자의 키가 파일에 들어 있지 않으므로, 팀장님이 AI 기능
  (질문 만들기 / 이력서 분석 / 공고 작성)을 쓰려면 **본인 키를 발급**받아야 합니다.
  (위 2번 순서를 그대로 안내해 주시면 됩니다)
- 담당자의 키를 알려 주어 함께 쓰는 것은 권하지 않습니다.
  키는 비밀번호와 같아서, 새어 나가면 다른 사람이 대신 쓸 수 있습니다.

### ② 면접만 보는 팀장님은 키가 없어도 됩니다

면접 질문을 담당자가 미리 만들어 **워드 파일로 배포**하는 방식이라면,
팀장님은 API 키 없이도 다음을 할 수 있습니다.

- 워드로 받은 질문지 보기 · 인쇄
- 앱에서 **후보자 평가 점수 입력과 저장**
- 평가 결과를 **엑셀로 내보내기**

즉 "팀장님은 앱을 열어 점수만 입력하고, 엑셀로 보내 주기"까지는 키가 필요 없습니다.
담당자는 받은 엑셀들을 **후보자 평가 → 📥 엑셀 가져와 합치기** 로 한 표에 모을 수 있습니다.

### ③ 인터넷 주소로 올려서 쓰는 경우 (넷리파이 등)

파일을 인터넷에 올려 주소로 접속하게 하려면, **`index.html` 이라는 이름의 파일이 반드시 있어야** 합니다.
없으면 접속했을 때 **404 Not Found** 오류가 납니다.
이 폴더에는 `index.html` 을 함께 넣어 두었으니, **폴더째 올리시면** 주소만 눌러도 화면이 열립니다.

올릴 때 알아 두실 점입니다.

- 앱 안에는 API 키가 들어 있지 않습니다. 접속한 사람이 **각자 자기 키를 넣어** 씁니다.
- 후보자 평가 기록은 **접속한 사람의 브라우저에 각각 따로** 저장됩니다.
  주소가 같아도 서로의 기록은 보이지 않습니다. (합칠 때는 엑셀을 주고받으세요)
- 다만 **주소를 아는 사람은 누구나 열 수 있습니다.** 원래 이 앱은 사내에서만 쓰는 것을 전제로 만들었으니,
  외부에 올리실 경우 사이트에 비밀번호를 걸거나 주소를 외부에 알리지 않는 것을 권합니다.

### ④ 각자의 기록은 각자 PC에만 남습니다

여러 사람이 같은 화면을 실시간으로 함께 보는 기능은 없습니다.
평가 결과를 모을 때는 반드시 **엑셀 주고받기** 를 이용해 주세요.

---

## 5. 자주 나는 오류 3가지와 해결법

### ① 화면은 뜨는데 질문이 안 만들어질 때

화면에 나오는 안내 문구를 먼저 확인해 주세요. 문구별로 원인이 다릅니다.

| 화면에 나오는 문구 | 원인과 해결법 |
|---|---|
| API 키가 올바르지 않습니다 | 키를 잘못 붙여 넣었습니다. 설정 화면에서 눈 아이콘(👁️)을 눌러 키를 확인하고, 앞뒤에 빈칸이나 따옴표가 섞이지 않았는지 보세요. 잘 모르겠으면 키를 지우고 2번 순서대로 다시 발급받아 붙여 넣는 것이 가장 빠릅니다. |
| 모델 이름을 찾을 수 없습니다 | 설정의 **모델 이름** 칸을 확인하세요. 기본값은 `gemini-3.5-flash-lite` 입니다. 지웠거나 오타가 났다면 이 값을 그대로 다시 적고 저장하세요. |
| 잠시 후 다시 시도해 주세요 (사용량 제한) | 짧은 시간에 너무 여러 번 요청했습니다. 1~2분 기다렸다가 다시 눌러 주세요. |
| 인터넷 연결을 확인해 주세요 | 인터넷이 끊겼거나 사내망이 구글 주소를 막고 있습니다. 다른 웹사이트가 열리는지 확인하고, 열린다면 전산 담당자에게 `generativelanguage.googleapis.com` 접속 허용을 요청해 주세요. |
| AI 응답을 읽지 못했습니다 | 가끔 AI가 형식을 어긴 답을 보낼 때가 있습니다. **한 번 더 눌러 주세요.** 대부분 두 번째에 성공합니다. |
| 먼저 설정에서 API 키를 넣어 주세요 | 키를 아직 저장하지 않았습니다. 설정 화면에서 키를 넣고 **저장하기** 를 누르세요. |

그 밖에 **직무기술서가 너무 짧으면** 질문 품질이 떨어집니다.
담당 업무와 자격 요건만이라도 넣어 주시면 결과가 훨씬 좋아집니다.

### ② 파일 업로드가 안 될 때

| 상황 | 해결법 |
|---|---|
| 파일 놓는 자리에 "이 PC에서는 파일 읽기 기능을 쓸 수 없습니다"가 보임 | 사내망이 외부 도구 주소를 막고 있는 경우입니다. **파일을 워드나 한글에서 열고 내용을 전체 선택(`Ctrl`+`A`) → 복사(`Ctrl`+`C`) → 앱의 큰 입력칸에 붙여넣기(`Ctrl`+`V`)** 하면 모든 기능을 그대로 쓸 수 있습니다. |
| "이 PDF에서는 글자를 찾지 못했습니다" | 스캔해서 만든 그림 PDF입니다. 글자를 뽑아낼 수 없으니 내용을 붙여넣어 주세요. |
| "예전 방식의 .doc 파일은 읽을 수 없습니다" | 워드에서 파일을 열고 `다른 이름으로 저장` → 형식을 **.docx** 로 바꿔 저장한 뒤 다시 올려 주세요. |
| 아무 반응이 없음 | 지원하는 형식은 **PDF · DOCX · TXT** 세 가지입니다. 한글(.hwp) 파일은 읽지 못하니 내용을 붙여넣어 주세요. |

> 파일 업로드는 어디까지나 **글자를 옮겨 적는 수고를 덜어 주는 기능**입니다.
> 안 되더라도 붙여넣기로 모든 기능을 쓸 수 있으니 걱정하지 않으셔도 됩니다.

### ③ 저장한 후보자 평가가 사라졌을 때

이 앱은 기록을 **브라우저 안**에 보관합니다. 그래서 아래 경우에 기록이 사라집니다.

| 원인 | 확인 방법과 해결법 |
|---|---|
| 브라우저 사용 기록(쿠키·사이트 데이터)을 지웠다 | 안타깝지만 되살릴 수 없습니다. 6번의 백업 방법을 꼭 실천해 주세요. |
| 다른 브라우저로 열었다 | 크롬에 저장한 기록은 엣지에서 보이지 않습니다. **처음 저장할 때 쓴 브라우저**로 다시 열어 보세요. |
| 시크릿 모드(InPrivate)로 열었다 | 시크릿 창은 닫는 순간 기록이 모두 지워집니다. 반드시 **일반 창**으로 여세요. |
| 파일을 다른 폴더로 옮겼다 | 파일 위치가 바뀌면 브라우저가 다른 앱으로 인식해 기록을 못 찾을 수 있습니다. **원래 있던 폴더로 파일을 되돌려 놓으면** 기록이 다시 보입니다. 옮길 계획이라면 먼저 엑셀로 내려받아 두세요. |
| 다른 PC에서 열었다 | 기록은 PC마다 따로 저장됩니다. 옮기려면 엑셀로 내보낸 뒤 새 PC에서 가져와 합치세요. |

---

## 6. 데이터 백업 방법 (꼭 읽어 주세요)

**후보자 평가 기록은 이 PC의 브라우저 안에만 있습니다.**
브라우저 기록을 지우거나 PC를 바꾸면 되살릴 수 없습니다.
그래서 **정기적으로 엑셀로 내려받아 보관**하시길 강하게 권합니다.

### 백업하는 방법 (1분이면 됩니다)

1. 왼쪽 메뉴에서 **📊 후보자 평가** 를 누릅니다.
2. 화면 아래쪽 **후보자 비교** 칸에서 **📊 엑셀로 내보내기** 를 누릅니다.
3. `후보자평가_20260811.xlsx` 같은 이름의 파일이 내려받기 폴더에 저장됩니다.
4. 이 파일을 회사 공용 폴더나 개인 문서 폴더에 옮겨 보관합니다.

### 언제 백업하면 좋은가

- **면접이 끝난 날마다 한 번** (가장 권장)
- 채용 한 건이 마무리되었을 때
- PC를 바꾸거나 브라우저를 정리하기 **전에** 반드시

### 백업한 엑셀을 다시 불러오는 방법

1. **📊 후보자 평가** 화면에서 **📥 엑셀 가져와 합치기** 를 누릅니다.
2. 보관해 둔 엑셀 파일을 고릅니다.
3. 같은 후보자 코드가 이미 있으면 **덮어쓸지 / 따로 추가할지** 물어봅니다.
   - **덮어쓰기** : 엑셀 내용으로 바꿉니다 (팀장님이 수정한 평가를 반영할 때)
   - **따로 추가** : 둘 다 남깁니다 (여러 면접관의 평가를 각각 보고 싶을 때)
   - **건너뛰기** : 지금 것을 그대로 둡니다

### 면접 질문지도 함께 보관하세요

면접 질문과 평가표는 **면접 질문 + 평가표** 화면에서
**워드**와 **엑셀**로 각각 내려받아 보관할 수 있습니다.
직무별로 한 번 만들어 두면 다음 채용 때 그대로 다시 쓸 수 있으니,
채용이 끝나면 두 파일을 함께 보관해 두시길 권합니다.

---

## 7. 개인정보 안내

- 이 앱은 **직무기술서와 이력서 내용만** 답변 생성을 위해 구글 Gemini로 보냅니다.
  그 외 모든 기록(설정, 후보자 평가)은 **이 PC 안에만** 저장되며 밖으로 나가지 않습니다.
- 이력서는 AI로 보내기 **전에** 휴대폰 번호, 이메일, 주민등록번호 형태의 숫자,
  생년월일, 주소를 자동으로 가립니다.
  가린 결과를 화면에서 먼저 보여 드리고, 확인을 누르셔야 전송됩니다.
- **지원자의 이름은 자동으로 가려지지 않습니다.**
  이력서를 붙여 넣기 전에 이름을 직접 지워 주세요.
- 이력서 원문은 앱에 저장되지 않습니다. 화면을 벗어나면 사라집니다.
- 후보자는 실명 대신 **후보자 코드**(A후보, 지원번호 등)로만 관리합니다.
- API 키는 이 브라우저에만 저장되며, 구글 외 어떤 곳으로도 보내지 않습니다.

---

## 8. 도움이 필요할 때

앱 화면에 나오는 안내 문구를 그대로 캡처해 두시면
문제를 훨씬 빨리 찾을 수 있습니다.
`F12` 같은 어려운 기능은 쓰지 않으셔도 됩니다.
